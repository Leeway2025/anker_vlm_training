# 下周工作明细(2026-08-03 ~ 08-08)

三条资源线并行:**TPU**(每晚一发训练)/ **CPU+网络**(1M 数据工程 + Gemini 标注)
/ **GPU 机**(重切帧)。每天格式:早=收账判读,白天=数据线,晚=TPU 点火。
纪律不变:单变量、全表验收、不涨即回退、产物落挂载盘、评测报告进 logs/。

---
## 周一 08-03

**早·收周末账(判读人:我)**
```bash
grep "SubKS\|RoleType" outputs/optin_kto/eval_report.txt outputs/optin_s12/eval_report.txt \
     outputs/jax_5b_seed2/eval_report.txt 2>/dev/null
tail -3 rat_rt.log        # RT重标: 完成量 + unsupported率(=懒标D浓度二审)
```
- KTO ≥73.84 或 双强汤 >73.77 → **单模交付口径刷新**,后续 BASE 全部指向新王;
- seed-2 成材(≥73.2)→ 我上午提交三模集成脚本,当场跑(CPU+一次15分钟dump)。

**白天·1M 开工(转发客户,CPU/网络)**
- P0 盘点:向数据方要四样——1M labels 路径、WDS 目录与大小、标注血统(谁标的/
  有无配平或翻译)、GPU 机档期 + Gemini 配额;
- 拿到路径立刻 P1:`bash scripts/profile_pool.sh /path/labels_1m_raw.jsonl`
- Gemini 排队:RT 重标(剩余)跑完 → 接 P2 盲判:
```bash
export GEMINI_API_KEY=AQ.xxx WDS_1M=<1M WDS目录>
nohup bash scripts/pool_sample_blind.sh /path/labels_1m_raw.jsonl > pool_blind.log 2>&1 &
```

**晚·TPU 过夜:RT 修复第一发**
```bash
BASE=<当前最强单模npz> nohup bash scripts/stage2_rtfix.sh > stage2_rtfix.log 2>&1 &
```
验收线:RT ≥ +0.3 且 SubKS 回撤 ≤ 0.2。

**我这边**:三模集成脚本、GPU 切帧参数建议书(24 vs 32/采样策略/200条切样清单)。

---
## 周二 08-04

**早**:收 stage2_rtfix 账(对 73.77/83.65 全表)。过线→上位并进 1M 默认配方;
不过→ rt-w/idw 出局,1M 配方维持 v4 原味。

**白天·CoT v2 备料(CPU)+ 1M 判读**
```bash
bash scripts/strip_cot_letters.sh                      # 82k 主资产去字母
# RT重标若完成: 合并 → asset_C_rtfocus.jsonl 也过一遍 strip
bash scripts/check_cot_labels.sh                       # 验收: 字母提取率应≈0
```
- P1 报告判读:中文 desc >5% → 触发翻译轮(P3.5,我出脚本);
- 盲判进度盯 `pool_blind.log` 的 ok 计数。

**晚·TPU 过夜:CoT v2 续训版**(命令我白天按当日 BASE 给成一键脚本)
- 靶:去字母化后隐式 CoT 是否转正;验收同全表规则。

**我这边**:train_sft 断点续训(checkpoint+optimizer 落盘/自动 resume)实现。

---
## 周三 08-05

**早**:收 CoT v2 账 → **100k 时代收官**:两发实验结论写死 1M 配方
(rt-w?idw?cot?),从此不再开 100k 训练槽。

**白天·1M 策展 + 质检落账(CPU)**
```bash
bash scripts/build_pool_v1.sh /path/labels_1m_raw.jsonl     # 出池+1M专属val卷
bash scripts/pool_blind_report.sh /path/labels_1m_raw.jsonl # 分层错标率地图
```
- 地图上"重点抽查"类 → 拉 20 条给客户肉眼(流程同 100k 抽查包);
- **GPU 侧开工**:按建议书切 200 条样片验证参数(客户执行)。

**客户沟通(关键)**:机时窗口谈定——全量一发 25~35h,关机策略必须让路,
或等断点续训验证完再开。

**晚·TPU**:`nohup bash scripts/smoke_1m.sh > smoke_1m.log 2>&1 &`
(P5 冒烟提前:用现有 16 帧 WDS 即可,重切帧的增益之后单变量验)。
若 P3 未就绪 → 备胎 seed-3 摇号(为集成攒原料)。

---
## 周四 08-06

**早**:收冒烟账——对 seed-1 裸分 73.52:±0.5 内放行;低 1 分以上暂停全量,
按盲判地图回查病类。

**白天**
```bash
bash scripts/check_wds_pool.sh /data/labels_1m_v1.jsonl <WDS目录>   # 覆盖+吞吐
```
- 断点续训 TPU 验证(短跑 200 步 → kill → resume → 等价性核对);
- GPU 切样 200 条人工验收 → 全量重切帧开闸(1~2 天,后台跑)。

**晚·TPU:全量首发点火**(三门全过:冒烟门/机时门/断点门)
```bash
nohup bash scripts/train_1m.sh > train_1m.log 2>&1 &   # 25-35h,跨周五
```
任一门未过 → 顺延一天,晚上补 seed-3/KTO-v2 等 2h 级弹药,不空槽。

---
## 周五 08-07

- **全量监工**:`tail -f train_1m.log` + metrics.jsonl val 曲线(异常即报);
- 周报三件套(战绩线 + 1M 工程进度 + 下周展望),口径规则照旧(只说集成);
- GPU 重切帧全量进度核对;
- 1M 首发预计周六出炉 → 周末动作预置:先验两折重标定 → RT 阈值 → 集成叠加,
  全部一键脚本现成,谁在机器旁谁转发。

---
## 固定纪律与后备弹药

- Gemini 单队列:RT 重标 → P2 盲判 → (触发则)翻译轮,不并发抢配额;
- TPU 晚间若空槽:优先级 seed-3 摇号 > KTO 二轮(换新 BASE 重挖配对);
- 每晚睡前:`pgrep` 存活确认 + 关键产物 `ls`;每早:报告落 logs/;
- 本周唯一不可妥协项:**断点续训未验证,全量不启动**。
